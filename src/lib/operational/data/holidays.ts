/**
 * @fileOverview Structured Holiday Rules (Phase 1 Skeleton).
 * To be populated via programmatic extraction in Phase 2.
 */
import { HolidayRule } from '../types';

export const HOLIDAY_RULES: Record<string, HolidayRule[]> = {
  // Skeleton ready for Phase 2 Extraction
};
